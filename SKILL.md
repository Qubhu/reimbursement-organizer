---
name: reimbursement-organizer
description: "整理报销发票材料：识别PDF发票内容和金额，按规则分类重命名（住宿、机票、火车、滴滴打车等），生成报销总结文档。当用户提到报销、发票整理、差旅报销等关键词时使用。触发词：报销、发票整理、报销材料、差旅报销、reimbursement。"
---

# 报销材料整理 Skill

## Overview

自动整理差旅报销发票材料：解压压缩包、逐个识别PDF内容和金额、按规则分类重命名、生成总结文档、汇总到统一文件夹。

## 关键原则

### ⚠️ PDF 必须逐个单独读取

**严禁并行读取多个 PDF 文件。** 并行 Read 返回的 PDF 文档顺序可能与调用顺序不一致，会导致文件内容与文件名错误关联。

```
❌ 错误做法：同时 Read 多个 PDF，假设返回顺序一致
✅ 正确做法：逐个 Read 每个 PDF，读完立即记录其内容和金额
```

### ⚠️ 基于内容命名，而非基于源文件名

不要信任原始文件名（如"滴滴电子发票.pdf"），必须读取实际内容后根据内容判断文件类型和金额。

## 完整流程

```
1. 扫描文件 → 2. 解压压缩包 → 3. 逐个识别PDF → 4. 分类命名 → 5. 汇总整理 → 6. 生成总结 → 7. 逐个验证
```

## Step 1: 扫描工作目录

```bash
ls -la <工作目录>/
```

识别所有文件类型：PDF文件、ZIP压缩包、已解压文件夹。

## Step 2: 解压所有压缩包

macOS 下使用 `ditto` 解压以正确处理中文文件名：

```bash
# ⚠️ 不要使用 unzip，中文文件名会乱码
ditto -x -k "压缩包.zip" "目标文件夹/"
```

每个 zip 解压到独立文件夹，避免文件覆盖。

## Step 3: 逐个识别每个 PDF

**必须一个一个读取**，每读完一个立即记录信息：

```
对于每个 PDF 文件：
  1. 单独 Read 该文件
  2. 识别类型：发票 / 行程单 / 车票
  3. 提取关键信息：
     - 金额（价税合计）
     - 日期
     - 类型（住宿/机票/火车/打车）
     - 路线/城市
  4. 立即记录到列表中，关联文件路径
```

### 发票类型识别规则

| 关键词 | 类型 |
|---|---|
| 住宿服务/住宿费 | 住宿发票 |
| 代订机票/经纪代理服务*代订机票 | 机票发票 |
| 铁路电子客票/火车票 | 火车票发票 |
| 运输服务*客运服务费 + 滴滴 | 滴滴发票 |
| 滴滴出行-行程单 | 滴滴行程单 |

### 滴滴打车分类规则

根据行程单中的**起点城市和终点城市**判断：

- **同城**（起终点在同一城市）→ 市内交通 → 命名前缀 `滴滴`
- **跨城**（起终点在不同城市）→ 在途交通 → 命名前缀 `在途滴滴`

判断方法：
- 看行程单中"城市"列和"起点/终点"列
- 如果终点地址包含其他城市名（如从苏州到上海），视为跨城
- 机场到市区（同城）仍属于市内交通

## Step 4: 文件重命名规则

| 类型 | 命名格式 | 示例 |
|---|---|---|
| 住宿发票 | `住宿发票_XXX.pdf` | 住宿发票_607.75.pdf |
| 机票发票 | `机票发票_XXX.pdf` | 机票发票_1210.00.pdf |
| 火车票发票 | `火车票发票_XXX.pdf` | 火车票发票_553.00.pdf |
| 市内滴滴行程单 | `滴滴行程单_XXX.pdf` | 滴滴行程单_130.80.pdf |
| 市内滴滴发票 | `滴滴发票_XXX.pdf` | 滴滴发票_130.80.pdf |
| 跨城滴滴行程单 | `在途滴滴行程单_XXX.pdf` | 在途滴滴行程单_309.32.pdf |
| 跨城滴滴发票 | `在途滴滴发票_XXX.pdf` | 在途滴滴发票_309.32.pdf |

其中 XXX 为价税合计金额，保留小数点。

## Step 5: 汇总到统一文件夹

```bash
mkdir -p <工作目录>/报销材料汇总
# 将所有已命名的文件复制到该文件夹
cp "源文件" "报销材料汇总/新文件名.pdf"
```

## Step 6: 生成总结文档

创建 `报销总结.txt`，包含以下内容：

```
1. 基本信息：报销人、出差时间、整理日期
2. 住宿费用：酒店名称、入住/退房日期、天数、金额、日期是否连续
3. 在途交通：机票/火车（日期、路线、金额）、跨城滴滴（日期、路线、金额）
4. 市内交通：按城市分组，列出日期、笔数、金额
5. 费用汇总表：各类别小计 + 总计
6. 材料清单：所有文件名、类型、序号
7. 出差行程概览：按日期梳理完整行程时间线
```

## Step 7: 逐个验证（必须执行）

**对每个已命名的文件逐个单独读取验证：**

```
对于 报销材料汇总/ 中的每个 PDF：
  1. 单独 Read 该文件
  2. 核对文件名中的金额与实际内容金额是否一致
  3. 核对文件名中的类型（发票/行程单）与实际内容是否一致
  4. 如有不一致，立即修正
```

⚠️ 验证时同样禁止并行读取，必须逐个进行。

## Step 8: 清理

```bash
# 删除原始文件、压缩包、临时解压文件夹
# 只保留 报销材料汇总/ 文件夹
# 确保无压缩文件残留
```

## 检查清单

- [ ] 所有 zip 已解压，无压缩文件残留
- [ ] 每个 PDF 单独读取识别，未使用并行读取
- [ ] 文件名金额与实际内容金额一致
- [ ] 文件名类型（发票/行程单）与实际内容一致
- [ ] 滴滴打车同城/跨城分类正确
- [ ] 每对滴滴（行程单+发票）金额匹配
- [ ] 总结文档完整（住宿、在途交通、市内交通、汇总）
- [ ] 所有文件集中在一个文件夹内
- [ ] 最终逐个验证全部通过
